# Reuse Knowledge

> 最近更新：2026-06-05

这里记录相邻项目、旧代码和历史工程经验的复用策略。它回答“哪些可以借，哪些不能直接搬，以及如何披露来源”。

## 当前入口

- `adjacent-projects.md`：`ai-visual-novel-engine` 与 `kmd` 的复用边界。

## 维护规则

- 这里记录复用事实和边界，不放具体 Phase 任务。
- 直接复用旧代码时，必须同步到 README 和 PR 描述的来源说明。
- 如果复用内容变成核心架构，应在 `../architecture/` 中补充当前项目自己的设计。

